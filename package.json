{
  "name": "fishaudio-morning-call",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Fish Audio Emotion Tags로 만드는 감정형 모닝콜",
  "scripts": {
    "voices": "node --env-file=.env src/voices.mjs",
    "rec": "node src/record.mjs",
    "render": "node src/render.mjs",
    "clone": "node --env-file=.env src/clone.mjs",
    "setup": "node src/setup.mjs",
    "build": "node --env-file=.env src/build.mjs",
    "say": "node --env-file=.env src/say.mjs",
    "start": "node --env-file=.env server/index.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
