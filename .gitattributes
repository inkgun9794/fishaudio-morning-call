# 기본은 LF. 맥에서 셔뱅이 CRLF로 깨지면 실행이 안 된다.
* text=auto eol=lf

# Windows 배치 파일만 CRLF
*.cmd text eol=crlf

# 셸 스크립트는 반드시 LF
*.command text eol=lf
*.sh      text eol=lf

# 바이너리
*.mp3 binary
*.wav binary
*.m4a binary
*.mp4 binary
*.exe binary
